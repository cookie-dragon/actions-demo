name: C/C++ CI

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]

jobs:
  build:

    runs-on: ubuntu-18.04

    steps:
    - name: show
      run: pwd
    - name: pwd
      run: pwd > pwd.out
