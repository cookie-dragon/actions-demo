name: test CI

on:
  push:
    paths: 
      - '.github/workflows/test.yml'
  workflow_dispatch:

jobs:

  job_test:
    name: Job - Test
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Step - Test
        run: |
          cat config.base.2019.02.11
        working-directory: ./htbox-forlinx/buildroot-config
