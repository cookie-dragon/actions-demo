name: test CI

on:
  push:
    paths: 
      - '.github/workflows/test.yml'
  workflow_dispatch:

jobs:

  job_test:
    name: Job - Test
    runs-on: ubuntu-18.04
    steps:
      # - name: Checkout
      #   uses: actions/checkout@v2

      # - name: Step - Test
      #   run: |
      #     touch a.txt
      #     touch b.txt

      #     mkdir -p rfs_runner/t
      #     cp a.txt rfs_runner/t/
      #     sudo cp b.txt rfs_runner/t/

      #     sudo mkdir -p rfs_root/t
      #     sudo cp b.txt rfs_root/t/

      #     tree -pug

      - name: Step - Test2
        run: |
          mkdir -p rfs_runner
          touch rfs_runner/a.txt
          sudo touch rfs_runner/b.txt
          tar zcf rfs_runner.tgz rfs_runner

          sudo mkdir -p rfs_root
          sudo touch rfs_root/b.txt
          sudo tar zcf rfs_root.tgz rfs_root

          mkdir tmp_runner1
          tar zxf rfs_runner.tgz -C tmp_runner1

          mkdir tmp_runner2
          sudo tar zxf rfs_runner.tgz -C tmp_runner2

          mkdir tmp_root1
          tar zxf rfs_root.tgz -C tmp_root1

          mkdir tmp_root2
          sudo tar zxf rfs_root.tgz -C tmp_root2

          tree -pug
