
name: CI

on:
#   push:
#     branches: [ dev ]
#   pull_request:
#     branches: [ dev ]
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-18.04
    
    steps:
    
      - name: STEP
        run: |
          wget https://buildroot.org/downloads/buildroot-2019.02.11.tar.gz
          tar zxf buildroot-2019.02.11.tar.gz
          cd buildroot-2019.02.11
          make busybox-menuconfig
          ls ./output/build/busybox-1.29.3
