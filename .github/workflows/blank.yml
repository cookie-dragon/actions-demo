
name: CI

on:
#   push:
#     branches: [ dev ]
#   pull_request:
#     branches: [ dev ]
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-18.04
    
    steps:
    
      - name: STEP
        run: |
          mkfs.ubifs
