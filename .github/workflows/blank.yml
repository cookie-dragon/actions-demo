
name: CI

on:
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-18.04
    
    steps:
    
      - name: STEP
        run: |
          wget https://invisible-mirror.net/archives/ncurses/6.1/ncurses-6.1-20190720.patch.gz
